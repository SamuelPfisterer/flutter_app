import 'package:flutter/material.dart';

class ClickersScreen extends StatelessWidget {
  const ClickersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Clickers Screen'),
      ),
    );
  }
} 